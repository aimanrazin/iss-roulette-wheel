export const STORAGE_KEYS = {
  CONFIG: "lucky_draw_config",
  DRAWN: "lucky_draw_drawn",
  REMAINING: "lucky_draw_remaining",
  HISTORY: "lucky_draw_history",
} as const;

export const SLIDER_RANGE_DIGITS = {
  MIN: 1,
  MAX: 20,
};
